# Holikar does not require environment variables.
# There are no API keys, tokens, or secrets to configure.
#
# Inspection data is stored locally in the user's browser (IndexedDB).
# Vercel only hosts the static frontend.
#
# If variables are added later, list them below as KEY=
